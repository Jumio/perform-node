import {FieldName} from '../../../enum/field-name';
import {FieldRules} from '../field-rules';

export const ruleMerchantReportingCritera: FieldRules = {
  fieldName: FieldName.merchantReportingCriteria,
  isRequired: false,
  maxLength: 100
};
