/**
 * Copy this file to "./config.ts" and populate with real values.
 */
import {Config} from './config-model';

export const config: Config = {
  apiToken: 'myToken',
  apiSecret: 'mySecret',
  userAgent: 'myCompany MyApp/v0.0.1'
};
